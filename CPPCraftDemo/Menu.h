#pragma once

namespace menu
{

struct Menu
{
	/*
		FindVectorRecords function follows the lookup logic of a vector as a data type of Record objects.
	*/
	static void FindVectorRecords();

	/*
		FindMapRecords function follows the lookup logic of a map as a data type of Record objects.
	*/
	static void FindMapRecords();
};

} // namespace menu
